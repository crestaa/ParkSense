#define ESPNOW_GATEWAY {0xF6, 0x12, 0xFA, 0x6A, 0x49, 0x90} // target ESP-NOW gateway (MAC address)

#define TIME_TO_SLEEP 20 // time device will go to sleep (in seconds)

// DHT config (leave as it is if not in use)
#define DHTPIN 23
#define DHTTYPE DHT22

// VL53L0X config (leave as it is if not in use)
#define SDA_PIN 18
#define SCL_PIN 19